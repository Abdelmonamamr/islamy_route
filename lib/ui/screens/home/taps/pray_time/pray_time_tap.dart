import 'package:flutter/material.dart';

class PrayTimeTap extends StatelessWidget {
  const PrayTimeTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.red,
    );
  }
}
